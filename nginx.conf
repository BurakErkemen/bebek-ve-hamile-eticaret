server {
    listen 80;
    server_name _;

    # Let's Encrypt doğrulama
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://app:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Ürün görselleri — uzun süreli cache
    location /uploads/ {
        proxy_pass http://app:3000/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# SSL aktif edildikten sonra aşağıdaki bloğu aç:
# server {
#     listen 443 ssl;
#     server_name senindomain.com;
#     ssl_certificate /etc/letsencrypt/live/senindomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/senindomain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://app:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
